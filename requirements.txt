# Semantic Geodesic Risk Fields
numpy>=1.24,<2.0
pyyaml>=6.0
scipy>=1.10
scikit-fmm>=2024.5.21
openai>=1.0
google-genai>=1.0.0   # for Gemini (Phase 0 LLM prior); replaces deprecated google-generativeai
anthropic>=0.40.0

# Loop 1 perception (RealSense, Lang-SAM/Grounded-SAM, Open3D) is optional — install per
# semantic_safety.perception_2d3d when wired; not listed here yet.
