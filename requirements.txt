# Semantic Geodesic Risk Fields
numpy>=1.24,<2.0
pyyaml>=6.0
scipy>=1.10
openai>=1.0
google-genai>=1.0.0   # for Gemini (Phase 0 LLM prior); replaces deprecated google-generativeai

# SONATA is installed separately (see README):
# - Clone https://github.com/facebookresearch/sonata
# - Install its deps (torch, spconv, etc.) and optionally: pip install -e ./sonata
